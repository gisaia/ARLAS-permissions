# Status: Ignored for now - action to be done: upgrade keycloak
# Reason: need keycloak dependecy update to 26.0.6
# Description: Potential Mitigations Requires access to the deployment's local network to exploit
CVE-2024-10039

# Status: Can be ignored
# Reason:  ARLAS-permissions uses Java which performs its own memory management
# Description : Potential Mitigations fromhttps://avd.aquasec.com/nvd/2024/cve-2024-13009/ : [...] languages such as Java, Ruby, and Lisp perform automatic garbage collection that releases memory for objects that have been deallocated
CVE-2024-13009


# Status: Can be ignored
# Reason: ARLAS-permissions runs behind reverse proxies / ingress controllers (Nginx, Traefik, Envoy) that validate and normalize HTTP/1.1 chunked extensions. Direct Jetty upgrade to 12.x requires full Dropwizard 5 migration.
# Description: Eclipse Jetty HTTP Request Smuggling via Chunked Extension Quoted-String Parsing
CVE-2026-2332

# Status: Can be ignored
# Reason: ARLAS-permissions does not use HTTP Digest Authentication. Authentication is strictly token-based (Bearer JWT / Keycloak / Auth0 / IAM). The vulnerability in Jetty's DigestAuthenticator/DigestAuthentication is unreachable.
# Description: Eclipse Jetty Authentication bypass via ISO-8859-1 lossy character encoding collision in HTTP Digest Authentication
CVE-2026-10050